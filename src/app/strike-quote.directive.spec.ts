import { StrikeQuoteDirective } from './strike-quote.directive';

describe('StrikeQuoteDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikeQuoteDirective();
    expect(directive).toBeTruthy();
  });
});
